# General UI glyphs — group A part 2 (dark neutral flat icons)

Continuation of the same batch as `PROMPT31_icons_group_a1.md` — SAME style,
read that file's "Style" section first and apply it identically here too
(flat solid dark-neutral #333438-class silhouette glyph, no outline/
gradient/shadow/texture/3D, centered with ~20% padding, transparent
background, exact square canvas size per icon). Save outputs into
`icons_general/` in this directory, same folder as part 1.

## Icons (18 files)

1. `puzzle_upvotes.png`, 128x128 — a thumbs-up hand icon.
2. `reset_key.png`, 64x64 — a circular arrow (counter-clockwise reset/undo
   arrow forming almost a full circle).
3. `save.png`, 64x64 — a classic floppy disk silhouette.
4. `settings.png`, 64x64 — a mechanical gear/cog (same concept as
   `main_menu_settings`, keep them visually consistent).
5. `settings_menu_exit.png`, 128x128 — a door outline with an arrow
   pointing out of it (exit), same concept as `main_menu_exit`.
6. `settings_menu_play.png`, 128x128 — a solid play triangle (▶).
7. `settings_menu_settings.png`, 128x128 — a mechanical gear/cog, same
   concept as `settings.png`.
8. `shop.png`, 64x64 — a simple shopping bag silhouette.
9. `sound_off.png`, 64x64 — a speaker cone with sound-off (a diagonal
   slash through it, no sound-wave arcs).
10. `sound_on.png`, 64x64 — the same speaker cone shape, with small
    curved sound-wave arcs beside it (no slash).
11. `state_back_button.png`, 64x64 — a bold left-pointing chevron/arrow.
12. `state_next_button.png`, 64x64 — a bold right-pointing chevron/arrow
    (mirror of the back button).
13. `statistics.png`, 64x64 — a simple bar chart (3 vertical bars of
    increasing height).
14. `unpin.png`, 32x32 — a map/push pin (same shape as `pin.png`) with a
    diagonal slash through it.
15. `unpin_shape.png`, 64x64 — a small rounded-square/shape outline with a
    diagonal slash through it (unpin a shape from a favorites list).
16. `waypoint.png`, 32x32 — a small flag on a short pole (waypoint
    marker).
17. `waypoint_wires.png`, 64x64 — the same small flag-on-pole shape as
    `waypoint.png`, with one small circular node/dot at the flag's base
    (marks a waypoint specifically on the wires/signal layer).
18. `current_goal_marker.png`, 32x32 — a simple crosshair/target glyph
    (concentric circle with a center dot), marks the player's current
    objective.

Generate all 18 as separate transparent PNGs at their listed sizes, named
exactly as given, saved in `icons_general/`.
